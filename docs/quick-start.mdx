---
title: Quick Start
sidebar_position: 0
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import CodeBlock from "@theme/CodeBlock";

# Quick Start

This guide will help you set up a project, deploy, make a change, test it, and then tear down the project.

## 0. Setup

- [Node JS 18+](https://nodejs.org/en/)
- NPM 7+ or Yarn 1+ or PNPM
- Create an [AWS Account](https://aws.amazon.com/) and set up your [AWS CLI](https://docs.aws.amazon.com/cli/latest/userguide/getting-started-install.html):

## 1. Create a new project

To create a new Eventual project, run:

<Tabs groupId="npm">
  <TabItem value="npm" label="npm" default>
    <CodeBlock>npm create eventual@latest</CodeBlock>
  </TabItem>
  <TabItem value="yarn" label="yarn">
    <CodeBlock>yarn create eventual@latest</CodeBlock>
  </TabItem>
  <TabItem value="pnpm" label="pnpm">
    <CodeBlock>pnpm create eventual@latest</CodeBlock>
  </TabItem>
</Tabs>

### Install Dependenices

<Tabs groupId="npm">
  <TabItem value="npm" label="npm" default>
    <CodeBlock>{"cd my-project\nnpm i"}</CodeBlock>
  </TabItem>
  <TabItem value="yarn" label="yarn">
    <CodeBlock>{"cd my-project\nyarn"}</CodeBlock>
  </TabItem>
  <TabItem value="pnpm" label="pnpm">
    <CodeBlock>{"cd my-project\npnpm i"}</CodeBlock>
  </TabItem>
</Tabs>

### Review the created project structure

The project template generates the following project structure containing 3 NPM packages:

```bash
my-service
├──README.md # a README with some helpful tips
├──infra # an AWS CDK application that deploys the repo's infrastructure
├──packages
    ├──service # the NPM package containing the @my-service/service business logic
```

## 2. Add a Command

Let's add a Command to the Service and then test it.

Add a command to the `packages/service/src/hello.ts`:

```ts
export const echo = command("echo", async (message) => {
  return message;
});
```

### Send a request

```
npx eventual echo "hello eventual"
```

## 3. Deploy the application

Use the `deploy` script to deploy your application to AWS.

<Tabs groupId="npm">
  <TabItem value="npm" label="npm" default>
    <CodeBlock>npm run deploy</CodeBlock>
  </TabItem>
  <TabItem value="yarn" label="yarn">
    <CodeBlock>yarn deploy</CodeBlock>
  </TabItem>
  <TabItem value="pnpm" label="pnpm">
    <CodeBlock>pnpm run deploy</CodeBlock>
  </TabItem>
</Tabs>

This will deploy to AWS using the AWS Cloud Development Kit.

## Get the Service's endpoints

To view the API Gateway URL and Event Bus ARN for a service, use the `eventual show service` command:

```ts
npx eventual show service
```

## Make an API HTTP request

Use a tool like `curl` to make an HTTP request to the API Gateway endpoint for the `/work` API. This will trigger a workflow execution:

```
curl -X POST <api-endpoint>/hello -d "sam"
```

This will return a JSON object containing the execution ID for the triggered workflow execution.

```json
{ "executionId": "<execution-id>" }
```

## Get the status of the Workflow execution

You can get the status and result of a workflow execution using the `eventual get execution` command:

```
npx eventual get execution <execution-id>
```

## View the logs for the execution

To view the logs for the workflow execution we just started, use the `eventual get logs` command followed by the `--execution` flag and the execution ID:

```
npx eventual get logs --execution <execution-id>
```

## Next Steps

- To learn more about our features and capabilities, view the [What is Eventual](./learn/1-what-is-eventual.md) 📖 guide.
- Review the [Reference Docs](./reference/service.md) for a deep dive into Eventual's API.
